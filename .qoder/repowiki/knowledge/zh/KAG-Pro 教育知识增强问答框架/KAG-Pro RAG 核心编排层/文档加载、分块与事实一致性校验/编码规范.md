- 对外暴露的实体统一封装为 `Document(text, metadata)`，metadata 作为 dict 透传并在各阶段追加字段（如 stage、subject、section、page）。
- 类方法采用 `@classmethod` 或实例方法分离纯函数逻辑（如 `_infer_subject`、`_infer_stage`、`_extract_claims`），便于单元测试替换。
- 配置型映射表以类属性形式定义在类体中（`_STAGE_FROM_PREFIX`、`_SUBJECT_KEYWORDS`、`_separators`），避免运行时重复构建。