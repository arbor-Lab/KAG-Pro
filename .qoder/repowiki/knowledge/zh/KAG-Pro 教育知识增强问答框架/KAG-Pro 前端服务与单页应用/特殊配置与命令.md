启动方式：`python frontend/server.py`，默认监听 `127.0.0.1:7860`；首次启动时 `get_pipeline()` 会清空并重建 `src/kag_pro/data/textbooks` 下的文档索引。批量生成教材：`python frontend/generate_textbooks.py`，输出文件形如 `gen_{stage}_{subj}_{i}.txt`。